# ./mupic ./tests/land-cover-one-border.mupic 

# landscape cover to square on landscape output with margin and border.
# but only one border side

output {
    size = 1920x1080
    path = ../test-out/land-cover-one-border.png
    color = white
}

image cover {
    size = max
    position = "output(min, min)"
    content {
        fit = fill, mid
        file = "resources/landscape-mode.png"
        mask = black
    }
    margin = 100
    border {
        width {l=100}
        color = "blue"
    }
}
